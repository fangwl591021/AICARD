{
  "name": "aicard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply aicard-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply aicard-db --remote",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260725.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.26.0"
  }
}
